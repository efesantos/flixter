<br />

<div class="booyah-box col-xs-10 col-xs-offset-1">
  <h1><%= current_lesson.title %></h1>
  <p>
    <%= current_lesson.subtitle %>
  </p>
  <% if current_lesson.video %>
    <%= video_tag current_lesson.video, class: 'img-responsive' %>
  <% end %>
</div>